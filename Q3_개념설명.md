# Q3 쓰기 정합성 유지 전략 - 개념 설명

> **문제**: 비정규화된 `shipments.current_status` 컬럼을 `shipment_updates` INSERT 시 어떻게 동기화할 것인가?

---

## 📋 전략 비교 요약

| 전략 | 방식 | Server Avg (1억) | 정합성 | 복잡도 |
|:----:|:----:|:----------------:|:------:|:------:|
| **sync** | 동기 트랜잭션 | 16.10ms | ✅ 완벽 | 낮음 |
| **trigger** | DB 트리거 | 12.45ms | ✅ 완벽 | 중간 |
| **async** | Kafka 비동기 | 12.04ms | ⚠️ Eventual | 높음 |

---

## 1️⃣ 동기 트랜잭션 (sync)

### 아키텍처

```
┌──────────┐     ┌──────────────┐     ┌──────────────────┐
│  Client  │────▶│   FastAPI    │────▶│   PostgreSQL     │
│          │     │    (API)     │     │                  │
└──────────┘     └──────────────┘     │  ┌────────────┐  │
                                      │  │ BEGIN      │  │
                                      │  │ INSERT     │  │
                                      │  │ UPDATE     │  │
                                      │  │ COMMIT     │  │
                                      │  └────────────┘  │
                                      └──────────────────┘
```

### 동작 흐름

| 단계 | 작업 |
|:----:|:-----|
| 1 | Client가 상태 변경 요청 |
| 2 | API가 트랜잭션 시작 (`BEGIN`) |
| 3 | `shipment_updates`에 **INSERT** |
| 4 | `shipments`에 **UPDATE** |
| 5 | 트랜잭션 커밋 (`COMMIT`) |
| 6 | Client에 응답 반환 |

### 코드 구현

```python
# shipments.py (sync 전략)
async def update_status_sync(shipment_id, status_code, db):
    # 1. 로그 INSERT
    new_update = ShipmentUpdate(
        shipment_id=shipment_id,
        status_code=status_code,
        timestamp=datetime.now()
    )
    db.add(new_update)
    
    # 2. 상태 UPDATE (같은 트랜잭션)
    shipment = db.query(Shipment).filter_by(shipment_id=shipment_id).first()
    shipment.current_status = status_code
    shipment.last_updated_at = datetime.now()
    
    # 3. 커밋 (둘 다 성공하거나 둘 다 실패)
    db.commit()
```

### 장단점

| 장점 | 단점 |
|:----:|:----:|
| ✅ 완벽한 즉시 일관성 | ❌ 트랜잭션 Lock 시간 증가 |
| ✅ 구현이 단순함 | ❌ 동시 처리량 감소 |
| ✅ 롤백 시 둘 다 복구 | ❌ 대용량에서 성능 저하 |

---

## 2️⃣ 데이터베이스 트리거 (trigger)

### 아키텍처

```
┌──────────┐     ┌──────────────┐     ┌──────────────────────────┐
│  Client  │────▶│   FastAPI    │────▶│      PostgreSQL          │
│          │     │    (API)     │     │                          │
└──────────┘     └──────────────┘     │  ┌────────────────────┐  │
                                      │  │ INSERT             │  │
                                      │  │   ↓                │  │
                                      │  │ 🔔 TRIGGER 자동 실행 │  │
                                      │  │   ↓                │  │
                                      │  │ UPDATE (DB가 처리)  │  │
                                      │  └────────────────────┘  │
                                      └──────────────────────────┘
```

### 동작 흐름

| 단계 | 작업 |
|:----:|:-----|
| 1 | Client가 상태 변경 요청 |
| 2 | API가 `shipment_updates`에 **INSERT만** 수행 |
| 3 | 🔔 DB 트리거가 자동으로 감지 |
| 4 | 트리거가 `shipments` **UPDATE** 실행 |
| 5 | Client에 응답 반환 |

### 코드 구현

**SQL 트리거 (schema/05_trigger_strategy.sql)**

```sql
-- 1. 트리거 함수 정의
CREATE OR REPLACE FUNCTION update_shipment_status_trigger()
RETURNS TRIGGER AS $$
BEGIN
    -- NEW: 방금 INSERT된 행
    UPDATE shipments
    SET 
        current_status = NEW.status_code,
        last_updated_at = NEW.timestamp
    WHERE shipment_id = NEW.shipment_id;

    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- 2. 트리거 생성
CREATE TRIGGER trg_update_shipment_status
AFTER INSERT ON shipment_updates
FOR EACH ROW
EXECUTE FUNCTION update_shipment_status_trigger();
```

**Python API (INSERT만)**

```python
# shipments.py (trigger 전략)
async def update_status_trigger(shipment_id, status_code, db):
    # INSERT만 하면 됨 - UPDATE는 트리거가 알아서 처리
    new_update = ShipmentUpdate(
        shipment_id=shipment_id,
        status_code=status_code,
        timestamp=datetime.now()
    )
    db.add(new_update)
    db.commit()  # 커밋 시 트리거 자동 실행!
```

### 장단점

| 장점 | 단점 |
|:----:|:----:|
| ✅ 앱 코드 단순화 | ❌ 로직이 DB에 숨겨짐 |
| ✅ DB 레벨 정합성 보장 | ❌ 디버깅 어려움 |
| ✅ 코드 변경 없이 자동화 | ❌ 대용량 INSERT 시 부하 |

---

## 3️⃣ 메시지 큐 비동기 처리 (async - Kafka)

### 아키텍처

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Client     │────▶│   FastAPI    │────▶│  PostgreSQL  │
│   (요청)      │     │   (API)      │     │  (INSERT만)  │
└──────────────┘     └──────┬───────┘     └──────────────┘
                           │
                           │ 📤 Kafka 발행
                           ▼
                   ┌───────────────┐
                   │    Kafka      │
                   │   (메시지 브로커) │
                   └──────┬────────┘
                          │
                          │ 📥 Consumer 구독
                          ▼
                   ┌───────────────┐     ┌──────────────┐
                   │   Consumer    │────▶│  PostgreSQL  │
                   │   (백그라운드)  │     │   (UPDATE)   │
                   └───────────────┘     └──────────────┘
```

### 동작 흐름

| 단계 | 주체 | 작업 |
|:----:|:----:|:-----|
| 1 | **Client** | 상태 변경 요청 (`POST /shipments/{id}/status`) |
| 2 | **API** | `shipment_updates`에 **INSERT만** 수행 |
| 3 | **API** | Kafka로 메시지 발행 후 **즉시 응답** 반환 |
| 4 | **Kafka** | 메시지를 토픽에 저장 |
| 5 | **Consumer** | 메시지 수신 → `shipments` 테이블 **UPDATE** |
| 6 | **Consumer** | Elasticsearch에도 동기화 (선택) |

### 코드 구현

**Producer (kafka_producer.py)**

```python
async def send_status_update(shipment_id, status_code, timestamp):
    """API가 호출하는 함수 - Kafka로 메시지 발행"""
    
    message = {
        "shipment_id": shipment_id,
        "status_code": status_code,
        "timestamp": timestamp,
        "event_type": "STATUS_UPDATE"
    }
    
    # Fire-and-Forget: 브로커 응답 안 기다림 → 빠름!
    await producer.send("shipment-status-updates", value=message)
```

**Consumer (kafka_consumer.py)**

```python
async def consume_status_updates():
    """백그라운드에서 실행 - 메시지 받아서 DB 업데이트"""
    
    async for msg in consumer:
        data = json.loads(msg.value)
        
        # shipments 테이블 UPDATE
        shipment.current_status = data["status_code"]
        shipment.last_updated_at = data["timestamp"]
        db.commit()
        
        # Elasticsearch 동기화
        await index_status_update(...)
```

### 장단점

| 장점 | 단점 |
|:----:|:----:|
| ⚡ API 응답 속도 가장 빠름 | ⏱️ 일시적 불일치 (Eventual Consistency) |
| 📊 DB 쓰기 부하 분산 | 🔧 Kafka 인프라 필요 |
| 🔄 Consumer 스케일 아웃 가능 | 🐛 Consumer 장애 시 지연 |

---

## 📊 벤치마크 결과

> **비교 기준:** Server Avg Time (순수 서버 처리 시간)

| 규모 | sync | trigger | async | 최적 전략 |
|:----:|:----:|:-------:|:-----:|:---------:|
| **300만 건** | 13.33ms | **8.21ms** | 9.15ms | trigger |
| **3,000만 건** | 12.74ms | **8.21ms** | 9.15ms | trigger |
| **1억 건** | 16.10ms | 12.45ms | **12.04ms** | async |

---

## ⚠️ 전략 선택 가이드

| 상황 | 추천 전략 | 이유 |
|:----:|:---------:|:-----|
| **정합성 최우선** | sync | 트랜잭션 보장 |
| **중소규모 (3,000만 이하)** | trigger | 가장 빠름, 단순함 |
| **대규모 (1억+)** | async | 부하 분산 효과 |
| **인프라 단순화** | trigger | Kafka 불필요 |
