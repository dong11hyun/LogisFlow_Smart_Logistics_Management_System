# 5단계 진행
pip install aiohttp


## 5-1 방안 1: PostgreSQL 파티션 쿼리 최적화

> ✅ 3단계에서 이미 구현됨

- `GET /shipments/{id}/timeline?source=postgresql`
- 파티션 테이블 (`shipment_updates`)에서 조회
- 인덱스 `idx_shipment_updates_shipment_timestamp` 활용

```python
# shipments.py - 타임라인 조회 (방안 1)
updates = db.query(ShipmentUpdate)\
    .filter(ShipmentUpdate.shipment_id == shipment_id)\
    .order_by(desc(ShipmentUpdate.timestamp))\
    .all()
```

---

## 5-2 방안 2: Elasticsearch 동기화 + 조회

### 구현된 파일

| 파일 | 역할 |
|------|------|
| `backend/app/elasticsearch_client.py` | ES 클라이언트 모듈 (인덱싱, 조회) |
| `backend/app/kafka_consumer.py` | Kafka Consumer에 ES 동기화 추가 |
| `backend/app/routers/shipments.py` | 타임라인 API에 ES 조회 옵션 추가 |
| `scripts/setup_elasticsearch.py` | ES 인덱스 초기화 스크립트 |
| `scripts/benchmark_q4.py` | Q4 벤치마크 스크립트 |

---

### 사용 방법

#### 1. Elasticsearch 인덱스 설정

```powershell
# Docker 컨테이너 확인
docker ps | findstr elasticsearch

# 인덱스 생성
python scripts/setup_elasticsearch.py
```

#### 2. 서버 재시작

```powershell
cd C:\B3_LogisFlow\backend
uvicorn app.main:app --reload --port 8000
```

#### 3. API 테스트

```powershell
# PostgreSQL 조회 (방안 1)
curl "http://localhost:8000/shipments/1/timeline?source=postgresql"

# Elasticsearch 조회 (방안 2)
curl "http://localhost:8000/shipments/1/timeline?source=elasticsearch"
```

#### 4. 벤치마크 실행

```powershell
python scripts/benchmark_q4.py
```

---

### 데이터 동기화 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                        Write Path                                │
├─────────────────────────────────────────────────────────────────┤
│  API (async/async_pure)                                          │
│       ↓                                                          │
│  Kafka Topic: shipment-status-updates                            │
│       ↓                                                          │
│  Kafka Consumer                                                  │
│       ├──────────────────┬──────────────────┐                   │
│       ↓                  ↓                  ↓                   │
│  PostgreSQL INSERT    PostgreSQL UPDATE   Elasticsearch INDEX   │
│  (shipment_updates)   (shipments)         (shipment-updates)    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        Read Path                                 │
├─────────────────────────────────────────────────────────────────┤
│  GET /shipments/{id}/timeline?source=<option>                    │
│                                                                  │
│  source=postgresql   →   PostgreSQL 파티션 테이블 조회           │
│  source=elasticsearch →   Elasticsearch 인덱스 조회             │
└─────────────────────────────────────────────────────────────────┘
```

---

### Elasticsearch 클라이언트 주요 함수

```python
# elasticsearch_client.py

# 타임라인 조회
async def get_timeline_from_es(shipment_id: int, limit: int = 100)

# 상태 업데이트 인덱싱
async def index_status_update(update_id, shipment_id, status_code, notes, timestamp)

# ES 연결 확인
async def check_es_connection()
```

---

### 예상 벤치마크 결과

| 방안 | 특징 | 예상 성능 |
|------|------|----------|
| **PostgreSQL** | 파티션 프루닝, 인덱스 활용 | 단순 조회 빠름 |
| **Elasticsearch** | 분산 검색, 풀텍스트 가능 | 대용량에서 유리 |

> 실제 성능은 데이터 양, 인덱스 설정, 하드웨어에 따라 다름
