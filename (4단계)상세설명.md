# 4단계 진행

#### 4-1 동기 트랜잭션      
→ ✅ 3-3에서 완료
#### 4-2 트리거 sql 작성
> schema/05_trigger_strategy.sql 생성

- 트리거 내용
함수: `update_shipment_status_trigger()`
동작: `shipment_updates`에 INSERT 발생 시 → `shipments` 테이블 자동 `UPDATE`
적용: 부모 테이블에 적용하면 모든 파티션에도 자동 적용됨 (PostgreSQL 기능)

```powershell
# 트리거 DB 생성 명령어
docker exec -it logisflow-postgres psql -U postgres -d logisflow -f /docker-entrypoint-initdb.d/05_trigger_strategy.sql
```
#### 4-3 트리거 전략 api 연결
```powershell
#api test
#cd backend 폴더! 에서 서버 실행.
uvicorn app.main:app --reload --port 8000

#db 조회 명령어. 
docker exec -it logisflow-postgres psql -U postgres -d logisflow -c "SELECT shipment_id, current_status, last_updated_at FROM shipments WHERE shipment_id = 1;"

#API 요청 트리거 테스트
curl -X POST "http://localhost:8000/shipments/1/status" -H "Content-Type: application/json" -d "{\"status_code\": \"IN_TRANSIT\", \"strategy\": \"trigger\", \"notes\": \"트리거테스트2\"}

```
```powershell
#db 조회 명령어. 최종적으로 IN_TRANSIT으로 바뀌었어야함.
docker exec -it logisflow-postgres psql -U postgres -d logisflow -c "SELECT shipment_id, current_status, last_updated_at FROM shipments WHERE shipment_id = 1;"

```

- API 요청을 하면 일어나는 일
1. API 호출: 사용자가 POST 요청을 보냄 (상태: IN_TRANSIT, 전략: trigger)
2. INSERT: 백엔드 API가 shipment_updates 테이블에 로그만 INSERT함
3. Trigger 실행: DB 내부의 트리거(trg_update_shipment_status)가 즉시 발동
4. UPDATE: 트리거가 shipments 테이블의 current_status를 자동으로 업데이트
5. 응답: API가 성공 응답을 반환