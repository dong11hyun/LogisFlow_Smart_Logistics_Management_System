# Q3 벤치마크 테스트 결과 보고서

> **작성일**: 2026-01-12  
> **프로젝트**: LogisFlow 스마트 물류 플랫폼  
> **목적**: Q3 쓰기 정합성 전략 5가지 성능 비교

---

## 📋 목차

1. [테스트 개요](#테스트-개요)
2. [구현된 전략](#구현된-전략)
3. [벤치마크 진행 과정](#벤치마크-진행-과정)
4. [오류 및 해결 과정](#오류-및-해결-과정)
5. [최종 벤치마크 결과](#최종-벤치마크-결과)
6. [결과 분석](#결과-분석)
7. [결론 및 권장사항](#결론-및-권장사항)

---

## 테스트 개요

### 테스트 환경

| 항목 | 값 |
|------|-----|
| OS | Windows |
| Python | 3.11 |
| Database | PostgreSQL 15 (Docker) |
| Message Broker | Apache Kafka 7.4.0 (Docker) |
| Backend | FastAPI + uvicorn |
| 벤치마크 도구 | Python requests + concurrent.futures |

### 테스트 설정

```python
TOTAL_REQUESTS = 500  # 전략당 요청 횟수
CONCURRENCY = 30      # 동시 요청 수
```

---

## 구현된 전략

### 전략 1: sync (동기 트랜잭션)

```python
# 하나의 트랜잭션에서 INSERT + UPDATE 모두 수행
db.add(new_update)  # INSERT INTO shipment_updates
shipment.current_status = status_code  # UPDATE shipments
db.commit()
```

- **특징**: 완벽한 즉시 일관성 보장
- **DB 작업**: SELECT + INSERT + UPDATE

---

### 전략 2: trigger (DB 트리거)

```sql
-- 05_trigger_strategy.sql
CREATE TRIGGER trg_update_shipment_status
AFTER INSERT ON shipment_updates
FOR EACH ROW
EXECUTE FUNCTION update_shipment_status_trigger();
```

```python
# API에서는 INSERT만 수행
db.add(new_update)  # INSERT만, 트리거가 UPDATE 처리
db.commit()
```

- **특징**: DB 레벨 자동화, 앱 코드 단순
- **DB 작업**: SELECT + INSERT (트리거가 UPDATE)

---

### 전략 3: async (Kafka 비동기 - DB INSERT 포함)

```python
# 1. DB에 로그 INSERT
db.add(new_update)
db.commit()

# 2. Kafka 메시지 발행
await send_status_update(shipment_id, status_code, timestamp)
```

- **특징**: Eventual Consistency, Consumer가 UPDATE 처리
- **DB 작업**: SELECT + INSERT + Kafka 전송

---

### 전략 4: async_pure (Kafka ONLY - INSERT 없음)

```python
# DB INSERT 없이 Kafka로만 발행
await send_status_update_pure(shipment_id, status_code, notes, timestamp)
```

- **특징**: API가 매우 빠름, Consumer가 INSERT + UPDATE 모두 처리
- **DB 작업**: SELECT + Kafka 전송

---

### 전략 5: async_fire (완전 Fire-and-Forget)

```python
# SELECT 쿼리도 없이 바로 Kafka 발행
await send_status_update_pure(shipment_id, status_code, notes, timestamp)
return response  # 즉시 반환
```

- **특징**: 최고 속도, 화물 존재 확인 없음
- **DB 작업**: Kafka 전송만

---

## 벤치마크 진행 과정

### 1차 테스트 (초기 설정)

**설정**: 100 요청, 10 동시성

```
Strategy   | TPS        | Avg (ms)   | P99 (ms)
--------------------------------------------------
sync       | 4.65       | 2125.25    | 2404.01
trigger    | 4.73       | 2089.27    | 2167.93
async      | 4.54       | 2199.49    | 2268.58
```

**문제 발견**: async가 가장 느림 (예상과 반대!)

**원인 분석**: `kafka_producer.py`에서 `send_and_wait()` 사용
- Kafka 브로커 응답을 기다리고 있었음

---

### 2차 테스트 (Fire-and-Forget 적용)

**수정 내용**:
```python
# Before
await producer.send_and_wait(topic, message)

# After
await producer.send(topic, value=message)
```

```
Strategy   | TPS        | Avg (ms)   | P99 (ms)
--------------------------------------------------
sync       | 4.67       | 2119.36    | 2333.60
trigger    | 4.70       | 2117.69    | 2194.33
async      | 4.75       | 2090.74    | 2159.96
```

**결과**: async가 가장 빨라짐 ✅

---

### 3차 테스트 (요청 수 증가)

**설정**: 500 요청, 30 동시성

```
Strategy   | TPS        | Avg (ms)   | P99 (ms)
--------------------------------------------------
sync       | 13.58      | 2152.97    | 2727.98
trigger    | 13.46      | 2164.71    | 2710.41
async      | 13.63      | 2125.96    | 2675.07
```

**문제**: 여전히 모든 전략이 비슷한 성능

**원인 분석**: 
- 모든 전략이 공통으로 SELECT + INSERT 수행
- DB 작업이 전체 시간의 99% 차지

---

### 4차 테스트 (async_pure 추가)

**async_pure 전략 추가**: DB INSERT 없이 Kafka만 발행

```
Strategy     | TPS        | Avg (ms)   | P99 (ms)
-------------------------------------------------------
sync         | 13.52      | 2158.00    | 3146.19
trigger      | 13.63      | 2135.68    | 2742.74
async        | 13.81      | 2110.93    | 2682.61
async_pure   | 13.96      | 2097.72    | 2624.74
```

**결과**: 여전히 비슷함

**원인 분석**: SELECT 쿼리가 병목

---

### 5차 테스트 (async_fire 추가 - 최종)

**async_fire 전략 추가**: SELECT도 없이 순수 Kafka만

**서버 처리 시간 측정 추가**

---

## 오류 및 해결 과정

### 오류 1: async 전략이 가장 느림

**증상**:
```
async      | 4.54       | 2199.49 ms   ← 가장 느림!
```

**원인**: `send_and_wait()` 사용

**해결**:
```python
# kafka_producer.py
# Before
await producer.send_and_wait(topic, message)

# After  
await producer.send(topic, value=message)
```

---

### 오류 2: async_pure 500 에러

**증상**:
```
🚀 Benchmarking Strategy: async_pure ...
❌ Error: 500 Server Error: Internal Server Error
```

**원인**: FakeUpdate 클래스의 속성 이름 충돌

```python
# 문제 코드
class FakeUpdate:
    update_id = 0
    timestamp = timestamp  # 변수명 충돌!
```

**해결**:
```python
# 수정된 코드
current_timestamp = datetime.now()
notes_value = request.notes or ""

class FakeUpdate:
    pass

new_update = FakeUpdate()
new_update.update_id = 0
new_update.shipment_id = shipment_id
new_update.status_code = request.status_code.value
new_update.notes = notes_value
new_update.timestamp = current_timestamp
```

---

### 오류 3: 모든 전략 성능이 비슷함

**증상**: 
- async_pure가 async_fire보다 빠르지 않음
- Client Latency가 모두 ~2000ms

**원인**: 
- 벤치마크 클라이언트(requests 라이브러리)의 연결 풀 병목
- 서버 처리 시간이 아닌 HTTP 왕복 시간 측정

**해결**: 서버 응답의 `processing_time_ms` 필드 활용

```python
# 수정된 벤치마크
data = resp.json()
server_time = data.get("processing_time_ms", 0)
```

---

## 최종 벤치마크 결과

### 설정

```
Requests: 500
Concurrency: 30
```

### 결과 테이블

| Strategy | TPS | Client Avg (ms) | Server Avg (ms) | Server P99 (ms) |
|:--------:|:---:|:---------------:|:---------------:|:---------------:|
| sync | 13.20 | 2199.12 | **13.33** | 47.10 |
| trigger | 13.65 | 2132.29 | **10.47** | 46.24 |
| async | 13.65 | 2129.37 | **10.69** | 47.74 |
| async_pure | 13.82 | 2112.54 | **4.22** | 30.50 |
| async_fire | 13.85 | 2111.03 | **4.14** | 33.40 |

### 서버 처리 시간 시각화

```
서버 처리 시간 비교 (Server Avg, 낮을수록 좋음)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
sync         ████████████████████████████  13.33ms
trigger      ██████████████████████        10.47ms
async        ██████████████████████        10.69ms
async_pure   █████████                      4.22ms
async_fire   █████████                      4.14ms ⚡
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 결과 분석

### 전략별 성능 비교

| 비교 | 시간 차이 | 개선율 | 원인 |
|------|:--------:|:------:|------|
| sync → trigger | -2.86ms | 21%↑ | 앱에서 UPDATE 제거 |
| sync → async | -2.64ms | 20%↑ | Kafka 비동기 전송 |
| async → async_pure | -6.47ms | **60%↑** | **INSERT 제거** |
| async_pure → async_fire | -0.08ms | 2%↑ | SELECT 제거 |

### 핵심 인사이트

1. **INSERT가 가장 무거운 작업** (6~7ms)
   - async → async_pure 전환 시 60% 성능 향상

2. **SELECT는 매우 빠름** (~0.1ms)
   - 인덱스가 잘 설정되어 있음

3. **트리거 ≈ async 성능**
   - 둘 다 앱에서 UPDATE를 처리하지 않음

4. **Client Latency ≠ 서버 성능**
   - 실제 서버는 4~13ms로 매우 빠름
   - 2000ms는 HTTP 클라이언트 병목

---

## 결론 및 권장사항

### 전략 선택 가이드

| 상황 | 추천 전략 | 서버 처리 시간 | 특징 |
|------|:--------:|:--------------:|------|
| **데이터 안정성 최우선** | sync | 13ms | 완벽한 즉시 일관성 |
| **DBA 중심 관리** | trigger | 10ms | 앱 코드 단순화 |
| **균형 (안정성+속도)** | async | 11ms | Eventual Consistency |
| **최고 속도 + 로그 비동기** | async_pure | 4ms | Consumer가 INSERT |
| **극한 속도 (검증 없음)** | async_fire | 4ms | 화물 존재 확인 없음 |

### 프로덕션 권장사항

1. **일반적인 경우**: `async` 전략
   - 안정성과 속도의 균형
   - 로그 데이터 즉시 저장

2. **고성능 필요 시**: `async_pure` 전략
   - 3배 빠른 응답 속도
   - Consumer 모니터링 필수

3. **트래픽 급증 대비**: `async_fire` 전략
   - API 응답 즉시 반환
   - 검증 로직을 Consumer로 이동

---

## 수정된 파일 목록

| 파일 | 수정 내용 |
|------|----------|
| `backend/app/routers/shipments.py` | async_pure, async_fire 전략 추가 |
| `backend/app/kafka_producer.py` | send_status_update_pure() 추가, Fire-and-Forget 적용 |
| `backend/app/kafka_consumer.py` | INSERT_AND_UPDATE 이벤트 처리 추가 |
| `scripts/benchmark_q3.py` | 서버 처리 시간 측정, 5가지 전략 테스트 |
| `schema/05_trigger_strategy.sql` | DB 트리거 정의 |

---

## 다음 단계

- [ ] 5단계: Q4 저장소 비교 (PostgreSQL 파티션 vs Elasticsearch)
- [ ] 6단계: 최종 벤치마크 및 문서화

---

> **문서 작성**: AI Assistant (Antigravity)  
> **검토**: LogisFlow 개발팀
