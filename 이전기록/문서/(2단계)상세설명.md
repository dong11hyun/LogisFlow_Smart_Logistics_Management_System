## 2단계 상세설명

#### 2-1 비정규화 스키마 설계
- 이미 1-2 에서 완료. 비정규화 스키마 구현.
`01_schema.sql`
```sql
CREATE TABLE shipments (
    -- 기존 컬럼들
    shipment_id SERIAL PRIMARY KEY,
    company_id INT,
    origin_warehouse_id INT,
    destination_warehouse_id INT,
    created_at TIMESTAMP,
    
    -- ⬇️ 비정규화 컬럼 (새로 추가) ⬇
    current_status VARCHAR(50),            -- 현재 상태
    last_updated_at TIMESTAMP,             -- 마지막 업데이트 시간
    origin_warehouse_name VARCHAR(100),    -- 출발 창고명
    destination_warehouse_name VARCHAR(100) -- 도착 창고명
);
```
---

#### 2-2 파티션 테이블 구성 (`02_partitioned_schema.sql`)
```powershell
# 파티션 스키마 적용
docker exec -it logisflow-postgres psql -U postgres -d logisflow -f /docker-entrypoint-initdb.d/04_partitioned_schema.sql

# 적용 확인
# 파티션 목록 확인
docker exec -it logisflow-postgres psql -U postgres -d logisflow -c "SELECT relname FROM pg_class WHERE relname LIKE 'shipment_updates%' ORDER BY relname LIMIT 10;"
```

- 일반테이블 (기존)
```
shipment_updates (하나의 거대한 테이블)
┌─────────────────────────────────────────────────────────┐
│  500억 건 데이터가 한 곳에!                              │
│  ├── 2020년 1월 데이터                                  │
│  ├── 2020년 2월 데이터                                  │
│  ├── ...                                                │
│  └── 2026년 1월 데이터                                  │
│                                                          │
│  조회: "2026년 1월 데이터 찾아줘"                         │
│  → 500억 건 전체를 스캔해야 함 (느림!)                   │
└─────────────────────────────────────────────────────────┘
```

- 파티션 테이블 (새로 만든 것)
```
shipment_updates (부모 - 논리적 테이블)
├── shipment_updates_2024_01 ──┬── 물리적으로 분리된 파일
├── shipment_updates_2024_02   │
├── ...                        │
├── shipment_updates_2025_12   │
└── shipment_updates_2026_01 ──┘  ← "2026년 1월 찾아줘" 
                                    → 이 파티션만 스캔! (빠름!)
```

- 실제 동작 예시
```sql
-- 2026년 1월 10일 데이터 INSERT
INSERT INTO shipment_updates (shipment_id, status_code, timestamp)
VALUES (1, 'DELIVERED', '2026-01-10 12:00:00');
PostgreSQL이 자동으로 timestamp를 보고 shipment_updates_2026_01 파티션에 넣어요!

-- 2026년 1월 데이터만 조회
SELECT * FROM shipment_updates 
WHERE timestamp >= '2026-01-01' AND timestamp < '2026-02-01';
```
>PostgreSQL이 자동으로 shipment_updates_2026_01 파티션만 스캔! (Partition Pruning)

---

#### 2-3 천만건 데이터 생성기

| 항목 | 기존 | 개선 |
| :--- | :--- | :--- |
| 포트 | `5432` | `5433` (로컬 PostgreSQL 충돌 방지) |
| DB명 | `shipment` | `logisflow` |
| 비밀번호 | 빈 값 | `logisflow1234` |

```diff
- 'port': 5432,
- 'dbname': 'shipment',
- 'password': '',
+ 'port': 5433,
+ 'dbname': 'logisflow',
+ 'password': 'logisflow1234',
```
```diff
- created_at = fake.date_time_between(start_date='-1y', end_date='now')
+ DATE_START = datetime(2024, 1, 1)
+ DATE_END = datetime(2026, 12, 31)
+ created_at = fake.date_time_between(start_date=DATE_START, end_date=DATE_END)
```

| 기존 (한글) | 개선 (영문) |
| :--- | :--- |
| 주문접수 | `PENDING` |
| 집화완료 | `PICKED_UP` |
| 터미널간이동 | `IN_TRANSIT` |
| 배송중 | `OUT_DELIVERY` |
| 배송완료 | `DELIVERED` |


```diff
# 기존은 shipments 테이블에 기본 컬럼만 INSERT했는데, 개선 버전은 비정규화 컬럼도 함께 INSERT합니다.

-- # 기존
INSERT INTO shipments (shipment_id, company_id, origin_warehouse_id, ...)

-- # 개선
INSERT INTO shipments (
    shipment_id, company_id, origin_warehouse_id, destination_warehouse_id, created_at,
+   current_status,           -- 비정규화
+   last_updated_at,          -- 비정규화
+   origin_warehouse_name,    -- 비정규화
+   destination_warehouse_name -- 비정규화
)
```

#### 2-4 데이터 생성 방법
```powershell
# 1. Docker 컨테이너 실행 확인
docker ps
# 만약 안 떠있으면:
cd C:\B3_LogisFlow
docker-compose up -d
# 2. PostgreSQL 정상인지 확인
docker exec -it logisflow-postgres psql -U postgres -d logisflow -c "SELECT 1;"
# 3. 데이터 생성기 실행
cd C:\B3_LogisFlow\data_generator
python generate_10m.py
```
```
🔌 연결 흐름
Python 스크립트 (로컬)
    ↓ localhost:5433
Docker PostgreSQL (logisflow-postgres)
    ↓
파티션 테이블에 데이터 저장
```
```powershell
#데이터 생성확인- 파티션테이블별
docker exec -it logisflow-postgres psql -U postgres -d logisflow -c "SELECT relname, n_live_tup FROM pg_stat_user_tables WHERE relname LIKE 'shipment_updates_2%' ORDER BY relname LIMIT 15;"

=========결과-dh====================
         relname          | n_live_tup 
--------------------------+------------
 shipment_updates_2024_01 |      84058
 shipment_updates_2024_02 |      81357
 shipment_updates_2024_03 |      86793
 shipment_updates_2024_04 |      85366
 shipment_updates_2024_05 |      88406
 shipment_updates_2024_06 |      83837
 shipment_updates_2024_07 |      87584
 shipment_updates_2024_08 |      88332
 shipment_updates_2024_09 |      84722
 shipment_updates_2024_10 |      87075
 shipment_updates_2024_11 |      85171
 shipment_updates_2024_12 |      86844
 shipment_updates_2025_01 |      87498
 shipment_updates_2025_02 |      77809
 shipment_updates_2025_03 |      87144
(15 rows)

```

```
README 지문 순서:         우리가 하고 있는 순서:
──────────────────        ─────────────────────
Q1. 병목 원인 분석        → 이건 "이론적 분석" (코드 X)
Q2. 비정규화 전략        → ✅ 1단계에서 완료 (01_schema.sql)
Q3. 정합성 3가지 전략    → 🔜 4단계에서 구현 예정
Q4. 조회 최적화 2가지    → 🔜 5단계에서 구현 예정
Q5. ILM 아카이빙        → 🔜 6단계에서 구현 예정
현재 진행 순서:

1단계: 인프라 구축 ✅
2단계: 데이터 파이프라인 ✅  ← 지금 여기!
3단계: 백엔드 API
4단계: Q3 정합성 전략 구현
5단계: Q4 저장소 비교
6단계: 벤치마크 및 문서화
```
```
지금 만든 것                →    테스트할 것들
───────────────────               ────────────────────
PostgreSQL + 파티션 테이블   →    Q3 전략 1,2,3 비교
                             →    Q4 방안 1 (파티셔닝) 테스트
Kafka 토픽                   →    Q3 전략 3 (비동기) 테스트
Elasticsearch 인덱스         →    Q4 방안 2 (ES) 테스트
100만 화물 + 300만 로그      →    벤치마크 대상 데이터
```

```
🔴 정규화 구조 (기존 - 느림)
┌─────────────────────────────────────┐
│           shipments                 │     ┌───────────────────────────────┐
│  ┌─────────────────────────────┐    │     │       warehouses              │
│  │ shipment_id: 1              │    │     │  ┌─────────────────────────┐  │
│  │ company_id: 5               │    │     │  │ warehouse_id: 10        │  │
│  │ origin_warehouse_id: 10  ───────────────▶ │ warehouse_name: "강남센터" │  │
│  │ destination_warehouse_id: 20│    │     │  └─────────────────────────┘  │
│  │ created_at: 2026-01-01      │    │     │  ┌─────────────────────────┐  │
│  │                             │    │     │  │ warehouse_id: 20        │  │
│  │ current_status: ???  ◀──────────────┐  │  │ warehouse_name: "부산센터" │  │
│  │                             │    │  │  │  └─────────────────────────┘  │
│  └─────────────────────────────┘    │  │  └───────────────────────────────┘
└─────────────────────────────────────┘  │
                                         │
┌─────────────────────────────────────┐  │
│      shipment_updates (500억 건!)   │  │
│  ┌─────────────────────────────┐    │  │
│  │ shipment_id: 1, PENDING     │    │  │
│  │ shipment_id: 1, PICKED_UP   │    │  │
│  │ shipment_id: 1, IN_TRANSIT  │    │  │  "현재 상태 뭐야?"
│  │ shipment_id: 1, DELIVERED ◀─────────┘  → 500억 건 중에서 찾아!
│  └─────────────────────────────┘    │     
└─────────────────────────────────────┘
문제: 현재 상태 조회할 때마다 500억 건 스캔 + JOIN 2번

🟢 비정규화 구조 (개선 - 빠름)
┌──────────────────────────────────────────────────────────────────┐
│                        shipments (비정규화)                       │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ shipment_id: 1                                              │ │
│  │ company_id: 5                                               │ │
│  │ origin_warehouse_id: 10                                     │ │
│  │ destination_warehouse_id: 20                                │ │
│  │ created_at: 2026-01-01                                      │ │
│  │ ─────────────────────────────────────────────────────────── │ │
│  │ ⭐ current_status: "DELIVERED"     ← 바로 여기!             │ │
│  │ ⭐ last_updated_at: 2026-01-03     ← 바로 여기!             │ │
│  │ ⭐ origin_warehouse_name: "강남센터" ← JOIN 필요없음!        │ │
│  │ ⭐ destination_warehouse_name: "부산센터" ← JOIN 필요없음!   │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
"현재 상태 뭐야?" → shipments 테이블만 조회! 끝!
📈 조회 성능 비교
┌────────────────────────────────────────────────────────────────────┐
│  정규화 쿼리 (느림)                                                 │
├────────────────────────────────────────────────────────────────────┤
│  SELECT s.shipment_id,                                             │
│         w1.warehouse_name as origin,        ← JOIN 1               │
│         w2.warehouse_name as destination,   ← JOIN 2               │
│         (SELECT status_code                                        │
│          FROM shipment_updates              ← 500억 건 서브쿼리!   │
│          WHERE shipment_id = s.shipment_id                         │
│          ORDER BY timestamp DESC LIMIT 1)                          │
│  FROM shipments s                                                  │
│  JOIN warehouses w1...                                             │
│  JOIN warehouses w2...                                             │
│                                                                    │
│  ⏱️ 예상 시간: 10초+                                               │
└────────────────────────────────────────────────────────────────────┘
┌────────────────────────────────────────────────────────────────────┐
│  비정규화 쿼리 (빠름)                                               │
├────────────────────────────────────────────────────────────────────┤
│  SELECT shipment_id,                                               │
│         origin_warehouse_name,              ← 이미 있음!           │
│         destination_warehouse_name,         ← 이미 있음!           │
│         current_status                      ← 이미 있음!           │
│  FROM shipments                                                    │
│                                                                    │
│  ⏱️ 예상 시간: 0.01초                                              │
└────────────────────────────────────────────────────────────────────┘
⚠️ 하지만! 새로운 과제 발생
상태가 바뀌면 두 곳을 동시에 업데이트해야 함!
     shipment_updates              shipments
     ┌────────────┐               ┌────────────────────┐
     │ INSERT     │   ──────▶     │ UPDATE             │
     │ (새 상태)  │   동기화      │ current_status     │
     └────────────┘   필요!       └────────────────────┘
이 "동기화"를 어떻게 할지? → Q3에서 3가지 방법 비교!
```